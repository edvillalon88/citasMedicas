{% extends 'base.html.twig' %}

{% block title %}Edit Doctor{% endblock %}

{% block body %}
    <h1 class="h3 mb-2 text-gray-800">Doctores</h1>
    <p class="mb-4"></p>

    <!-- DataTales Example -->
    <div class="card shadow mb-4">
    <div class="card-header py-3 ">
        <h5 class="m-0 font-weight-bold text-primary" style="display:inline">Editar</h5>
        
        <div class="float-right">
            {{ include('doctor/_delete_form.html.twig') }}
        </div>
        <a href="{{ path('doctor_index') }}" class="btn btn-secondary float-right mr-1" >Ir al Listado</a>
    </div>
    <div class="card-body">
        
        {{ include('doctor/_edit_form.html.twig') }}
            
        
    </div>
    </div>
{% endblock %}
