{% extends 'base.html.twig' %}

{% block title %}Consultorio index{% endblock %}

{% block body %}
    <h1>Consultorio index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for consultorio in consultorios %}
            <tr>
                <td>{{ consultorio.id }}</td>
                <td>{{ consultorio.nombre }}</td>
                <td>
                    <a href="{{ path('consultorio_show', {'id': consultorio.id}) }}">show</a>
                    <a href="{{ path('consultorio_edit', {'id': consultorio.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('consultorio_new') }}">Create new</a>
{% endblock %}
