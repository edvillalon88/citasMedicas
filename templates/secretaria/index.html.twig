{% extends 'base.html.twig' %}

{% block title %}Secretaria index{% endblock %}

{% block body %}
    <h1>Secretaria index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for secretarium in secretarias %}
            <tr>
                <td>{{ secretarium.id }}</td>
                <td>
                    <a href="{{ path('secretaria_show', {'id': secretarium.id}) }}">show</a>
                    <a href="{{ path('secretaria_edit', {'id': secretarium.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('secretaria_new') }}">Create new</a>
{% endblock %}
