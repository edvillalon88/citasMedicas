{% extends 'base.html.twig' %}


{% block body %}

    <h1 class="h3 mb-2 text-gray-800">Citas</h1>
    <p class="mb-4"></p>

    <!-- DataTales Example -->
    <div class="card shadow mb-4">
    <div class="card-header py-3 ">
        <h5 class="m-0 font-weight-bold text-primary" style="display:inline">Editar</h5>    
        
        <a href="{{ path('citas_index') }}" class="btn btn-secondary float-right mr-1" >Ir al Listado</a>
    </div>
    <div class="card-body">        
        {{ include('citas/_form.html.twig') }}
    </div>
    </div>
{% endblock %}
